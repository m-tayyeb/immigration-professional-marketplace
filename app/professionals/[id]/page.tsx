import Link from "next/link";
import { notFound } from "next/navigation";
import { ConsultationRequest } from "../../../components/consultation-request";
import { profileDetails, professionals } from "../../../lib/professionals";

export default async function ProfilePage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params; const professional = professionals.find((item) => item.id === id); if (!professional) notFound(); const details = profileDetails(professional); const initials = professional.name.split(" ").map((name) => name[0]).join("");
  return <main className="min-h-screen bg-mist"><header className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 lg:px-8"><Link href="/" className="flex items-center gap-2 text-xl font-bold"><span className="grid h-8 w-8 place-items-center rounded-lg bg-ocean text-sm text-white">M</span>Migrate</Link><Link href="/" className="text-sm font-semibold text-slate-600 hover:text-ocean">Return to homepage</Link></header><div className="mx-auto max-w-6xl px-6 py-5 lg:px-8"><Link href={`/professionals?country=${encodeURIComponent(professional.country)}&service=${encodeURIComponent(professional.specializations[0])}`} className="text-sm font-semibold text-ocean">← Back to search results</Link><section className="mt-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex flex-col gap-6 md:flex-row md:items-start md:justify-between"><div className="flex gap-4"><div aria-label={`${professional.name} avatar`} className="grid h-20 w-20 shrink-0 place-items-center rounded-2xl bg-teal-100 text-2xl font-bold text-ocean">{initials}</div><div><div className="flex flex-wrap items-center gap-2"><h1 className="text-3xl font-bold tracking-tight">{professional.name}</h1><span className="rounded-full bg-teal-100 px-2.5 py-1 text-xs font-bold text-teal-800">✓ Verified identity</span></div><p className="mt-1 text-slate-600">{professional.type}</p><p className="mt-2 text-sm text-slate-600">Practices in {details.practiceCountries.join(", ")} · {details.location}</p><p className="mt-3 max-w-2xl leading-6 text-slate-600">{details.tagline}</p></div></div><ConsultationRequest professional={professional} /></div><div className="mt-7 grid grid-cols-2 gap-3 border-t border-slate-200 pt-6 sm:grid-cols-4"><Stat label="Experience" value={`${professional.experience} years`} /><Stat label="Completed cases" value={details.completedCases.toString()} /><Stat label="Client rating" value={`★ ${professional.rating}/5`} /><Stat label="Reviews" value={professional.reviews.toString()} /></div></section><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_310px]"><div className="space-y-6"><Section title="About"><p>{details.biography}</p><h3 className="mt-5 font-bold text-ink">Professional credentials</h3><ul className="mt-2 list-disc space-y-1 pl-5">{professional.qualifications.map(item => <li key={item}>{item}</li>)}</ul></Section><Section title="Services"><div className="grid gap-4 sm:grid-cols-2">{details.services.map(service => <article key={service.name} className="rounded-xl border border-slate-200 p-4"><h3 className="font-bold">{service.name}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{service.description}</p><div className="mt-4 text-sm"><p><strong>From €{service.price}</strong></p><p className="mt-1 text-slate-500">{service.duration}</p></div><div className="mt-4"><ConsultationRequest professional={professional} service={service.name} /></div></article>)}</div></Section><Section title="Client reviews"><div className="space-y-4">{details.reviews.map(review => <article key={`${review.client}-${review.date}`} className="rounded-xl bg-[#EDF7F7] p-4"><div className="flex justify-between gap-3"><h3 className="font-bold">{review.client}</h3><span className="text-sm font-semibold text-ocean">{"★".repeat(review.rating)} <span className="text-slate-400">{review.date}</span></span></div><p className="mt-2 text-sm leading-6">“{review.text}”</p></article>)}</div></Section></div><aside className="space-y-5"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-bold uppercase tracking-[.15em] text-ocean">Availability</p><div className="mt-4 space-y-3 text-sm"><p><strong>Today:</strong> {details.availability.today ? "Available for a consultation" : "No appointments remaining"}</p><p><strong>This week:</strong> {details.availability.thisWeek}</p><p><strong>Next available:</strong> {details.availability.next}</p></div><div className="mt-5"><ConsultationRequest professional={professional} /></div></section><Section title="Languages spoken"><Tags items={professional.languages} /></Section><Section title="Immigration categories"><Tags items={professional.specializations} /></Section><Section title="Destination countries"><Tags items={details.practiceCountries} /></Section></aside></div></div></main>;
}
function Stat({ label, value }: { label: string; value: string }) { return <div className="rounded-xl bg-slate-50 p-3"><p className="text-lg font-bold text-ink">{value}</p><p className="text-xs font-medium text-slate-500">{label}</p></div>; }
function Section({ title, children }: { title: string; children: React.ReactNode }) { return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">{title}</h2><div className="mt-4 leading-7 text-slate-600">{children}</div></section>; }
function Tags({ items }: { items: string[] }) { return <div className="flex flex-wrap gap-2">{items.map(item => <span key={item} className="rounded-full bg-[#EDF7F7] px-3 py-1 text-sm text-ocean">{item}</span>)}</div>; }
